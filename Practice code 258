let arr = [10, 20, 30, 40];
let newArr = [];

for (let i = 0; i < arr.length; i++) {
  if (i === 2) {
    newArr.push(25); // insert after 2nd element
  }
  newArr.push(arr[i]);
}

console.log(newArr);

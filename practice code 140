let rows = 5;

for (let i = 1; i <= rows; i++) {
  let line = "";

  // spaces
  for (let s = rows - i; s > 0; s--) {
    line += " ";
  }

  // alphabets
  for (let j = 0; j < i; j++) {
    line += String.fromCharCode(65 + j) + " ";
  }

  console.log(line);
}

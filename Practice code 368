let arr = [1, 2, 4, 3, 4];

for (let i = 0; i < arr.length; i++) {
  if (arr[i] % 2 === 0)
    console.log("Even:", arr[i]);
  else
    console.log("Odd:", arr[i]);
}let arr = [10, 20, 30];
arr.splice(2, 0, 25);
console.log(arr);let arr = [10, 20, 30];
arr.splice(2, 0, 25);
console.log(arr);
